import { describe } from "node:test";

/**
 * Rung 3 — conformance tests.
 *
 * Later tickets will add tests here that assert CLI and MCP return
 * identical envelopes for the same inputs.
 */
describe("rung 3 conformance (placeholder)", () => {
  // empty — passes with exit 0
});
